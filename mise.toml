[env]
_.file = ".env"

[tools]
node = "24"
pnpm = "11"

[tasks.test]
description = "Run all repository tests and type checks"
run = [
	"pnpm test",
]

[tasks."pre-commit"]
description = "Execute before commit"
run = """
pnpm run check
pnpm typecheck
pnpm test
pnpm docs:check
"""

[hooks]
postinstall = "pnpm install && mise generate git-pre-commit --write --task=pre-commit"