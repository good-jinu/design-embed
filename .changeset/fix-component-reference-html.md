---
"@design-embed/target-react": patch
"@design-embed/target-vue": patch
---

Fix component visual spec to generate a dedicated `{ComponentName}.reference.html` fixture instead of reusing the parent view's reference HTML and extracting the element at runtime.
