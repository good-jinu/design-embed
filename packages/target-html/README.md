# @design-embed/target-html

HTML target support for design-embed.

It provides HTML-oriented emission behavior for debug and inspection workflows. The target is useful when validating parsed design input, preserving generated HTML for review, or building local integration checks before moving to framework-specific output.
